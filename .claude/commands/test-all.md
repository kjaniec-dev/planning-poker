Run the complete test suite across all components of the Planning Poker application:

1. Run Golang WebSocket server tests (18 tests)
2. Run Node.js WebSocket server tests (20+ tests)
3. Run React client tests (WebSocket client and useRealtime hook)
4. Generate coverage reports for each component
5. Provide a summary of test results and coverage

Execute all tests, report any failures, and highlight any coverage gaps that need attention.
